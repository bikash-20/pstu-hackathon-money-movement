# Base URL of the backend API. Trailing "/api" is optional — lib.ts normalizes
# it, so both forms below work identically.
NEXT_PUBLIC_API_URL=http://localhost:4000/api
# Production: https://pstu-hackathon-backend.onrender.com
